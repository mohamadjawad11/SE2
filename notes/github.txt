git remote -v  =>to check weather the project is connected to a github repo


Push Existing code to github repo:

git init
git remote add origin https://github.com/your-username/your-repo-name.git
git add .
git commit -m "Initial commit"
git push -u origin main


git rm --cached <Filename>          to unstage a file I don't want to commit and push

git log                             to see previous logs

git commit -a -m "Initial commit"      commit without staging like no need to git add .


git clone <repolink>                  to doenload the public repo on my machine


















